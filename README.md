# Elpan PostgreSQL

Custom PostgreSQL container images with pre-installed extensions, based on the official PostgreSQL Docker images.

## Features

This project extends the official PostgreSQL images with the following extensions:

- **PostGIS** - Spatial and geographic objects support

**Note:** Extensions are installed but not enabled by default. You can enable them as needed using `CREATE EXTENSION`.
